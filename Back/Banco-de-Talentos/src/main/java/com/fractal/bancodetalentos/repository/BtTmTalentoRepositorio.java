package com.fractal.bancodetalentos.repository;

import com.fractal.bancodetalentos.model.entity.BtTmTalento;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.query.Procedure;

import javax.persistence.ParameterMode;

public interface BtTmTalentoRepositorio extends JpaRepository<BtTmTalento, Integer> {
/*
    @Procedure(name = "ADMIN.SP_ADD_TALENT")
    Number addNewTalent(String, String, String);
    String.class, ParameterMode.IN)
            String.class, ParameterMode.IN)
               String.class, ParameterMode.IN)
                byte[].class, ParameterMode.IN)
                String.class, ParameterMode.IN)
               String.class, ParameterMode.IN)
                String.class, ParameterMode.IN)
               Number.class, ParameterMode.IN)
                Number.class, ParameterMode.IN)
                .registerStoredProcedureParameter(10, Number.class,
*/
}
