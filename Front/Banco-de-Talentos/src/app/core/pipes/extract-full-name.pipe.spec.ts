import { ExtractFullNamePipe } from './extract-full-name.pipe';

describe('ExtractFullNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractFullNamePipe();
    expect(pipe).toBeTruthy();
  });
});
